import React from "react";
import { Alert } from "@mui/material";

const ErrorAlert = () => {
    return <Alert severity="error">Виникла помилка, спробуйте оновити сторінку!</Alert>;
};

export default ErrorAlert;
